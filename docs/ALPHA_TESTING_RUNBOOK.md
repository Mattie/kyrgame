# Alpha Testing Runbook

The portable Docker and tunnel workflow now lives in `docs/DEV_HOSTING.md`.

Keep hostnames, named Compose projects, tunnel credentials, local backup paths,
and machine-specific restart notes in ignored local notes such as `.agents.md`.
Tracked docs should use placeholders like `<your-tunnel-host>` when an operator
must supply a private value.
